# Examples

Generated reports, ZIP bundles, SQLite databases, and graph images are not kept
in source control by default.

Use tests or local smoke scripts to create anonymized samples under
`examples/generated/` when needed. That directory is ignored so personal network
evidence is not accidentally published.
